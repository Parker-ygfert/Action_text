<div>
  <%= form_for(post) do |f| %>
    <div>
      <%= f.label :title %>
      <%= f.text_field :title %>
    </div>

    <div>
      <%= f.label :body %>
      <%= f.rich_text_area :body %>
    </div>

    <div>
      <%= f.submit %>
    </div>
  <% end %>
</div>