<h1>Post index</h1>

<p><%= link_to "New Post", :new_post %></p>

<table>
  <thead>
    <tr>
      <td>Title:</td>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= link_to post.title, post_path(post) %></td>
      </tr>
    <% end %>
  </tbody>
</table>